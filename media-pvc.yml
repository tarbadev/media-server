apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-claim
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 5Gi
  selector:
    matchLabels:
      name: "media-volume"